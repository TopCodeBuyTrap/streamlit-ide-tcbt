instalações e mosulos e pendencias para oprojeto

# atualiza o gerenciador de pacotes, setuptools e wheel essenciais para instalação de pacotes
python.exe -m pip install --upgrade pip
python -m pip install --upgrade setuptools wheel

# ---------------------------- criar app para executavel em pc
pip install streamlit-desktop-app
name: streamlit-desktop-app
version: 0.3.3
summary: easily convert streamlit apps into standalone desktop applications.
home-page: https://github.com/ohtaman/streamlit-desktop-app
author: ohtaman
author-email: ohtamans@gmail.com


intalei agora = pip install streamlit-monaco-editor
intalei agora = pip install streamlit-code-editor


# editor de codigos
pip install streamlit-ace==0.1.1



pip install streamlit-aggrid

streamlit run app_.py
streamlit run teste.py





# falta implemtar :
1. salvar arquivos automaticamente
todo código que o usuário digitar deve ser salvo automaticamente em uma pasta específica.
essa pasta vai armazenar todos os arquivos de todos os projetos que o usuário criar.
funciona como um repositório local de projetos da ide.

2. botão para baixar arquivos
implementar uma função que permita ao usuário baixar qualquer arquivo criado ou usado na ide.
o usuário poderá escolher o local de destino no computador.

3. pré-instalação de módulos ao criar um projeto
ao criar um novo projeto, a ide deve instalar automaticamente módulos ou bibliotecas que o projeto precisa.
isso garante que o projeto já esteja configurado e pronto para rodar sem ajustes manuais.

uma parte que eu apanhei muito, precisei até de trocar o editor de código, porque não estava acompanhando o tema que eu passava. quando eu fui olhar na função, estava errada, estava retornando quando eu entrava com o arquivo, eu coloquei uma função para retornar o que que era o arquivo para para modificar para a linguagem. só que a função estava retornando essa linguagem em que a primeira letra maiúscula, em em título, título. e isso estava fodendo com o tema. aí o tema não reconhecia a linguagem e não dava conta de de acompanhar. esse foi um problema, eu fiquei o dia inteiro para resolver e só fui descobrir depois que eu instalei uns dois ou três editores. outra coisa também que foi canseira foi de usar o terminal. ainda estou trabalhando nisso, mas é muito difícil de usar o terminal, fazer eh usar o o dos ou sei lá o que que é que tá usando aqui do do windows. e outra coisa também que foi difícil foi fazer o conectar o o preview do editor de código com o python, que está instalado na venv


# para criar o executavel:

streamlit-desktop-app build app_.py `
  --name tcbt_stream_ide `
  --streamlit-options --theme.base=dark `
  --pyinstaller-options `
    --onedir --noconsole --noconfirm `
    --collect-all streamlit `


nao coloquei
  --icon icon.ico `


    --add-data "logo.png;." `
    --add-data "qrcode.png;."

